{
  "name": "sabo-cat-app",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
      "start": "node server.js"
  },
  "engines": {
      "node": "18.x" // Specify your Node.js version (check with `node -v` locally)
  },
  "dependencies": {
      "express": "^4.18.2",
      "axios": "^1.6.8"
  }
}