{
  "name": "sabo-cat",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"  // Replace with your local Node version (check with `node --version`)
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.16.0"
  }
}